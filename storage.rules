rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /photos/{allPaths=**} {
      // Anyone can read uploaded photos (they're public)
      allow read: if true;
      // Only admin can upload or delete
      allow write: if request.auth != null && request.auth.token.admin == true;
    }
  }
}
